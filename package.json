{
  "name": "cryptoauto",
  "version": "1.0.0",
  "scripts": {
    "build": "echo 'No build required' && exit 0",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "dependencies": {
    "stripe": "^15.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260715.1",
    "wrangler": "^4.111.0"
  }
}
